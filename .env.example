##############################################
# 💡 模型服务配置（必填）
##############################################

# OpenAI / Claude / DeepSeek 等模型平台的 API Key
OPENAI_API_KEY=your-api-key-here

# 模型调用接口地址（默认 OpenAI，也可换成 DeepSeek / Zhipu 等）
OPENAI_BASE_URL=https://api.openai.com/v1

# 使用的模型名称，例如 gpt-4、gpt-4o、gpt-3.5-turbo
MODEL_NAME=gpt-4


##############################################
# 🛠 GitHack MCP 工具配置（本地脚本）
##############################################

# githack.py 的绝对路径（如 D:/Tools/githack.py）
GITHACK_PATH=/absolute/path/to/githack.py

# Python 解释器路径（默认可用 python，特殊环境可用 python3.11 等）
PYTHON_PATH=python


##############################################
# 🛠 sqlmap MCP 工具配置（脚本或系统 PATH 中的 sqlmap.py）
##############################################

# sqlmap.py 的绝对路径（如 git 克隆后的 sqlmap/sqlmap.py）
SQLMAP_PATH=/absolute/path/to/sqlmap.py


##############################################
# 🛠 xscan MCP 工具配置（本地可执行文件）
##############################################

# xscan 的绝对路径（例如 D:/Tools/xscan.exe 或 ./xscan）
XSCAN_PATH=/absolute/path/to/xscan
